export interface Book {
    title: string;
    author: string;
    available: boolean;
  }
// models/book.ts
export interface Book {
  name: string;
  category: string;
  price: number;
}
export interface Book {
  title: string;
  name: string;
  author: string;
  category: string;
  price: number;
  available: boolean;
}

